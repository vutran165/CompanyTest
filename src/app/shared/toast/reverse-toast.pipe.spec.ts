import { ReverseToastPipe } from './reverse-toast.pipe';

describe('ReverseToastPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseToastPipe();
    expect(pipe).toBeTruthy();
  });
});
